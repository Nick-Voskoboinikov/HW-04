CREATE TABLE "user" (
    "id" SERIAL PRIMARY KEY,
    "username" varchar(30),
    "password" varchar(32),
    "email" varchar(30),
    "created" varchar(30),
    "role" varchar(30),
    "hash" varchar
);
